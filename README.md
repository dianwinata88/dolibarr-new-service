# dolibarr-new-service

Standalone CRM microservice extracted from the Dolibarr ERP monolith (strangler-fig migration).

API-only service. Stack: PHP 8.3, Symfony 7, API Platform 4, Doctrine ORM, FrankenPHP, MariaDB.

See `docs/CONVENTIONS.md` for directory and contribution conventions.
