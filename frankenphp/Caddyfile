{
	{$CADDY_GLOBAL_OPTIONS}

	frankenphp {
		{$FRANKENPHP_CONFIG}
	}

	# HTTPS is handled by the reverse proxy in front of this service
	auto_https off
}

{$CADDY_EXTRA_CONFIG}

{$SERVER_NAME:localhost} {
	log

	root /app/public
	encode zstd br gzip

	{$CADDY_SERVER_EXTRA_DIRECTIVES}

	php_server
}
